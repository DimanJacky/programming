import React from 'react';

export const App = () => {
    // Задание
    // Что выведет этот скрипт?

    let name = "Ilya";

    console.log( `hello ${1}` ); // ?

    console.log( `hello ${"name"}` ); // ?

    console.log( `hello ${name}` ); // ?

    return (
        <div>
            <h1>Типы данных</h1>
            <p>Шаблонные строки</p>
        </div>
    );
};